{
  "scripts": {
    "generate": "prisma generate",
    "deploy":   "prisma deploy",
    "token":    "prisma token",
    "reset":    "prisma reset --force",
    "seed":     "prisma deploy"
  },
  "dependencies": {
    "faker": "^4.1.0",
    "prisma": "^1.34.10",
    "prisma-client-lib": "^1.34.10"
  }
}
